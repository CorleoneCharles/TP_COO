#ifndef "REGRESSION_H"
#define "REGRESSION_H"

#include "similarite.h"
#include "Deformation.h"
  class regressionfinale{ 
public:
virtual regressionfinale{ NRmatrix <int>& Image1,NRmatrix <int>& Image2, float * transformé}//Image 1 est l'image déformées, Image2, celle non déformée, transformé pointe sur un vecteur contenant 
// les paramètres de la transformation

#endif
